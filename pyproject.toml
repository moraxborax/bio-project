[project]
name = "bio-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.0",
    "numpy>=2.4.6",
    "scipy>=1.18.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.22",
]
